/**
 * SPEC.md §11 — the gear dialog with exactly one field, "Editor command" (default `code`),
 * backed by the §7 `get_settings` / `set_settings` commands.
 *
 * Implemented by plan 005 (M5). Intentionally empty at M1.
 */
export {};
