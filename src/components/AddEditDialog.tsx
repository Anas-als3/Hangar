/**
 * SPEC.md §10 — folder picker, script picker, port suggestion, duplicate-port validation,
 * url override, confirm-and-stop before saving a running project.
 *
 * Implemented by plan 005 (M5 — Add/Edit/Remove). Intentionally empty at M1.
 */
export {};
