/**
 * SPEC.md §11 signature element — the slim `Pull → Install → Start → Ready` strip along the
 * card's bottom edge, lit in amber as each real phase completes, skipped phases dimmed.
 *
 * Implemented by plan 006 (M6 — update-on-run + full UI pass). Intentionally empty at M1.
 */
export {};
