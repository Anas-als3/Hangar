/**
 * SPEC.md §11 — the log slide-over: mono font, autoscroll with pause-on-scroll-up, Copy, Clear,
 * stderr tinted, `system` lines muted, Esc closes.
 *
 * Implemented by plan 002 (M2 — spawn + logs). Intentionally empty at M1.
 */
export {};
